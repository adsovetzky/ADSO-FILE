#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    long long N, Q, L, R; cin >> N;
    vector<long long> S(N);

    for(long long i = 0; i < N; i++){
        cin >> S[i];
    }
    cin >> Q;
    while(Q--){
        cin >> L >> R;
        for(long long i = L; i <= R; i++){
            
        }
    }
}